[build.processing.html]
  # Disable Netlify's automatic trailing-slash redirect for directory-style URLs
  # (e.g. blog/index.html). Without this, Netlify redirects /blog → /blog/,
  # which conflicts with the /*/ → /:splat rule in _redirects and causes
  # an infinite redirect loop.
  pretty_urls = false
