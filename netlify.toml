[build.processing.html]
  # Disable Netlify's automatic trailing-slash redirect for directory-style URLs
  # (e.g. blog/index.html). Keep this disabled because Nuxt already emits
  # canonical URLs without trailing slashes.
  pretty_urls = false
